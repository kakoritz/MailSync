# Allow plaintext auth in test environment (never use in production)
disable_plaintext_auth = no
auth_mechanisms = plain login

passdb {
  driver = static
  args = password=testpass
}

userdb {
  driver = static
  args = uid=vmail gid=vmail home=/var/mail/%u
}
